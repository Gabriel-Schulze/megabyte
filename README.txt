DEPENDENCIAS:
    - mysql.connetor-python
    - customtkinter
    - pillow
    - matplotlib

OBSERVAÇÕES:
    - Para iniciar o codigo, rode o arquivo "telaLogin.py";
    - Desenvolvemos a aplicação com o python versão 3.12, talvez versões mais recentes quebrem a aplicação;
    - O arquivo com a conexão ao banco se chama "utils.py", os inserts, selects, deletes e updates estão nos arquivos crud, criamos um para cada tabela no banco(5 tabelas);
